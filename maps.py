# 2-D maps for trajectory generation

class free_map():
	def __init__(size, start, goal):
		self.map_size = size
		self.start = start
		self.goal = goal


class obstacle_map():
	def __init__(size, start, goal):
		self.map_size = size
		self.start = start
		self.goal = goal

